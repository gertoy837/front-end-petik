const users = {
    name: "Budi Santoso",
    age: 20,
    major: "Informatics",
};

// destructing object berdasarkan key
const {name, age, major} = users;

const nama = users.name;
const umur = users.age;
const jurusan = users.major;

console.log(nama);